const httpCode = require('./httpCode.constant');
const httpReason = require('./httpReason.constant');
const role = require('./role.constant');
const prefix = require('./prefix.constant');
const appMode = require('./appMode.constant');

module.exports = {
    httpCode,
    httpReason,
    role,
    prefix,
    appMode
};