const UserService = require('./user.service');
const AdminService = require('./admin.service');
const GlobalService = require('./global.service');

const services = {
    UserService, AdminService, GlobalService
}

module.exports = services;